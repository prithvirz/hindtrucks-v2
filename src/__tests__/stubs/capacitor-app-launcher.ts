// ESM stub for @capacitor/app-launcher
export const AppLauncher = {
    canOpenUrl: async (_options: { url: string }) => ({ value: true }),
    openUrl: async (_options: { url: string }) => ({ completed: true }),
}