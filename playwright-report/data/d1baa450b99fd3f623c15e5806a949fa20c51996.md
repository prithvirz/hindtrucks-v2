# Instructions

- Following Playwright test failed.
- Explain why, be concise, respect Playwright best practices.
- Provide a snippet of code with the fix, if possible.

# Test info

- Name: splash.spec.ts >> Splash Screen >> landing card has correct branding elements
- Location: e2e\splash.spec.ts:44:5

# Error details

```
Error: expect(locator).toBeVisible() failed

Locator: getByText(/earn/i).first()
Expected: visible
Timeout: 3000ms
Error: element(s) not found

Call log:
  - Expect "toBeVisible" with timeout 3000ms
  - waiting for getByText(/earn/i).first()

```

```yaml
- img
- heading "HindTrucks" [level=1]
- paragraph: Loads on every road
- heading "Driver Application" [level=2]
- paragraph: Logistics and load matching simplified
- img
- paragraph: High-Paying Verified Loads
- paragraph: Direct load matching near your location
- img
- paragraph: Instant Wallet Withdrawals
- paragraph: Receive payouts directly to your bank account
- img
- paragraph: Bharat Freight Club Elite Benefits
- paragraph: Exclusive leaderboards and bonus incentives
- button "Get Started":
  - text: Get Started
  - img
```

# Test source

```ts
  1  | import { test, expect } from '@playwright/test';
  2  | 
  3  | test.describe('Splash Screen', () => {
  4  |     test.beforeEach(async ({ page }) => {
  5  |         // Clear localStorage to ensure fresh state
  6  |         await page.goto('/');
  7  |         await page.evaluate(() => localStorage.clear());
  8  |     });
  9  | 
  10 |     test('displays splash animation on load', async ({ page }) => {
  11 |         await page.goto('/');
  12 |         // Should show the app logo / splash content
  13 |         await expect(page.locator('text=HindTrucks')).toBeVisible({ timeout: 3000 });
  14 |     });
  15 | 
  16 |     test('shows landing card with Get Started button after animation', async ({ page }) => {
  17 |         await page.goto('/');
  18 |         // Wait for the landing card to appear after 1.5s animation
  19 |         const getStarted = page.getByRole('button', { name: /get started/i });
  20 |         await expect(getStarted).toBeVisible({ timeout: 4000 });
  21 |     });
  22 | 
  23 |     test('Get Started button navigates to language picker', async ({ page }) => {
  24 |         await page.goto('/');
  25 |         const getStarted = page.getByRole('button', { name: /get started/i });
  26 |         await expect(getStarted).toBeVisible({ timeout: 4000 });
  27 |         await getStarted.click();
  28 |         await expect(page).toHaveURL(/\/language/);
  29 |     });
  30 | 
  31 |     test('auto-redirects to home when already logged in', async ({ page }) => {
  32 |         // Set auth flag before navigating
  33 |         await page.goto('/');
  34 |         await page.evaluate(() => {
  35 |             localStorage.setItem('ht_auth', '1');
  36 |             localStorage.setItem('ht_registered_9876543210', '1');
  37 |             localStorage.setItem('ht_tour', '1');
  38 |         });
  39 |         await page.goto('/');
  40 |         // Should redirect to /home after 1.5s
  41 |         await page.waitForURL(/\/home/, { timeout: 5000 });
  42 |     });
  43 | 
  44 |     test('landing card has correct branding elements', async ({ page }) => {
  45 |         await page.goto('/');
  46 |         await page.waitForTimeout(2000); // Wait for landing card
  47 |         // Verify key branding text
  48 |         await expect(page.getByText(/drive/i).first()).toBeVisible({ timeout: 3000 });
> 49 |         await expect(page.getByText(/earn/i).first()).toBeVisible({ timeout: 3000 });
     |                                                       ^ Error: expect(locator).toBeVisible() failed
  50 |     });
  51 | });
```