# Instructions

- Following Playwright test failed.
- Explain why, be concise, respect Playwright best practices.
- Provide a snippet of code with the fix, if possible.

# Test info

- Name: login-flow.spec.ts >> Login Flow >> complete login flow: splash → language → login → otp → home
- Location: e2e\login-flow.spec.ts:9:5

# Error details

```
Error: expect(locator).toBeVisible() failed

Locator: locator('input[type="tel"], input[placeholder*="phone" i], input[placeholder*="mobile" i], input[placeholder*="number" i]').first()
Expected: visible
Timeout: 8000ms
Error: element(s) not found

Call log:
  - Expect "toBeVisible" with timeout 8000ms
  - waiting for locator('input[type="tel"], input[placeholder*="phone" i], input[placeholder*="mobile" i], input[placeholder*="number" i]').first()

```

```yaml
- heading "Enter your mobile number" [level=1]
- paragraph: We'll send you a one-time code (OTP)
- text: Mobile number
- img
- text: "+91"
- textbox "98765 43210"
- img
- text: By continuing you agree to our Terms & Privacy Policy
- button "Send OTP" [disabled]
```

# Test source

```ts
  1   | import { test, expect } from '@playwright/test';
  2   | 
  3   | test.describe('Login Flow', () => {
  4   |     test.beforeEach(async ({ page }) => {
  5   |         await page.goto('/');
  6   |         await page.evaluate(() => localStorage.clear());
  7   |     });
  8   | 
  9   |     test('complete login flow: splash → language → login → otp → home', async ({ page }) => {
  10  |         // 1. Splash → Language
  11  |         await page.goto('/');
  12  |         const getStarted = page.getByRole('button', { name: /get started/i });
  13  |         await expect(getStarted).toBeVisible({ timeout: 4000 });
  14  |         await getStarted.click();
  15  |         await expect(page).toHaveURL(/\/language/);
  16  | 
  17  |         // 2. Language → Login
  18  |         const continueBtn = page.getByRole('button', { name: /continue/i });
  19  |         await expect(continueBtn).toBeVisible();
  20  |         await continueBtn.click();
  21  |         await expect(page).toHaveURL(/\/login/);
  22  | 
  23  |         // 3. Enter phone number
  24  |         const phoneInput = page.locator('input[type="tel"], input[placeholder*="phone" i], input[placeholder*="mobile" i], input[placeholder*="number" i]').first();
> 25  |         await expect(phoneInput).toBeVisible();
      |                                  ^ Error: expect(locator).toBeVisible() failed
  26  |         await phoneInput.fill('9876543210');
  27  |         await phoneInput.blur();
  28  | 
  29  |         // 4. Click Send OTP
  30  |         const sendOtp = page.getByRole('button', { name: /send otp|get otp|continue/i });
  31  |         await expect(sendOtp).toBeEnabled({ timeout: 2000 });
  32  |         await sendOtp.click();
  33  | 
  34  |         // 5. OTP screen
  35  |         await expect(page).toHaveURL(/\/otp/, { timeout: 5000 });
  36  |         // OTP inputs are 4 digits
  37  |         const otpInputs = page.locator('.otp-digit, input[maxlength="1"], [data-testid="otp-input"]');
  38  |         const otpCount = await otpInputs.count();
  39  |         // Fill each OTP digit
  40  |         if (otpCount >= 4) {
  41  |             for (let i = 0; i < 4; i++) {
  42  |                 await otpInputs.nth(i).fill('1');
  43  |             }
  44  |         } else {
  45  |             // Fallback: find all inputs on the page
  46  |             const inputs = page.locator('input');
  47  |             const inputCount = await inputs.count();
  48  |             // Type into the OTP area
  49  |             await page.keyboard.type('1111');
  50  |         }
  51  | 
  52  |         // 6. Should navigate to home after OTP verify
  53  |         // The OTP screen auto-verifies when 4 digits are filled
  54  |         await page.waitForURL(/\/home/, { timeout: 10000 });
  55  |         await expect(page.locator('#driver-profile, [data-testid="driver-profile"]')).toBeVisible({ timeout: 5000 });
  56  |     });
  57  | 
  58  |     test('phone input validation: rejects short numbers', async ({ page }) => {
  59  |         await page.goto('/language');
  60  |         await page.waitForTimeout(500);
  61  |         const continueBtn = page.getByRole('button', { name: /continue/i });
  62  |         await expect(continueBtn).toBeVisible({ timeout: 3000 });
  63  |         await continueBtn.click();
  64  |         await expect(page).toHaveURL(/\/login/);
  65  | 
  66  |         const phoneInput = page.locator('input[type="tel"], input[placeholder*="phone" i], input[placeholder*="mobile" i], input[placeholder*="number" i]').first();
  67  |         await phoneInput.fill('12345');
  68  |         await phoneInput.blur();
  69  | 
  70  |         // Send OTP button should be disabled
  71  |         const sendOtp = page.getByRole('button', { name: /send otp|get otp|continue/i });
  72  |         await expect(sendOtp).toBeDisabled({ timeout: 2000 });
  73  |     });
  74  | 
  75  |     test('phone input validation: accepts valid 10-digit number', async ({ page }) => {
  76  |         await page.goto('/language');
  77  |         await page.waitForTimeout(500);
  78  |         const continueBtn = page.getByRole('button', { name: /continue/i });
  79  |         await expect(continueBtn).toBeVisible({ timeout: 3000 });
  80  |         await continueBtn.click();
  81  |         await expect(page).toHaveURL(/\/login/);
  82  | 
  83  |         const phoneInput = page.locator('input[type="tel"], input[placeholder*="phone" i], input[placeholder*="mobile" i], input[placeholder*="number" i]').first();
  84  |         await phoneInput.fill('9876543210');
  85  |         await phoneInput.blur();
  86  | 
  87  |         const sendOtp = page.getByRole('button', { name: /send otp|get otp|continue/i });
  88  |         await expect(sendOtp).toBeEnabled({ timeout: 2000 });
  89  |     });
  90  | 
  91  |     test('navigates to registration for new user phone', async ({ page }) => {
  92  |         // Use a phone that triggers registration (not in the pre-registered list)
  93  |         await page.goto('/language');
  94  |         await page.waitForTimeout(500);
  95  |         const continueBtn = page.getByRole('button', { name: /continue/i });
  96  |         await expect(continueBtn).toBeVisible({ timeout: 3000 });
  97  |         await continueBtn.click();
  98  |         await expect(page).toHaveURL(/\/login/);
  99  | 
  100 |         const phoneInput = page.locator('input[type="tel"], input[placeholder*="phone" i], input[placeholder*="mobile" i], input[placeholder*="number" i]').first();
  101 |         await phoneInput.fill('9999999999');
  102 |         await phoneInput.blur();
  103 | 
  104 |         const sendOtp = page.getByRole('button', { name: /send otp|get otp|continue/i });
  105 |         await expect(sendOtp).toBeEnabled({ timeout: 2000 });
  106 |         await sendOtp.click();
  107 | 
  108 |         await expect(page).toHaveURL(/\/otp/, { timeout: 5000 });
  109 | 
  110 |         // Fill OTP
  111 |         const otpInputs = page.locator('input[maxlength="1"]');
  112 |         const otpCount = await otpInputs.count();
  113 |         if (otpCount >= 4) {
  114 |             for (let i = 0; i < 4; i++) {
  115 |                 await otpInputs.nth(i).fill('1');
  116 |             }
  117 |         } else {
  118 |             await page.keyboard.type('1111');
  119 |         }
  120 | 
  121 |         // Should redirect to /register for new users
  122 |         await page.waitForURL(/\/register/, { timeout: 10000 });
  123 |         await expect(page.getByText(/create account/i)).toBeVisible({ timeout: 5000 });
  124 |     });
  125 | 
```