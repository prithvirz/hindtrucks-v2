# HindTrucks API Configuration
VITE_API_BASE_URL=/api
VITE_API_MODE=mock

# OpenRouteService routing key (https://openrouteservice.org/dev — free, no card).
# When set, trip routing uses ORS (driving-hgv); otherwise it falls back to OSRM.
VITE_ORS_API_KEY=
